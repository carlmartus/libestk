dist: xenial
language: c

addons:
    apt:
        packages:
            - libglew-dev
            - libsdl2-dev
            - libsdl2-image-dev
            - libsdl2-mixer-dev

script:
    - cmake .
    - cmake --build .
